{
  "name": "m-montreal-arrivals-counter-script",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "npx esbuild src/index.ts --bundle --outfile=dist/index.js --platform=node --minify --sourcemap",
    "test": "vitest run"
  },
  "author": "Chris Cormier",
  "license": "ISC",
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
